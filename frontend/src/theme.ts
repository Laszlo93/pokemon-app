import { createTheme } from "@mui/material/styles";

export const appTheme = createTheme({
  palette: {
    background: {
      default: "#f0f0f0",
      paper: "#fafafa",
    },
    primary: {
      main: "#2e7d32",
    },
  },
});
